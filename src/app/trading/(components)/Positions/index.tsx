import React from 'react';
import GridItem from '@/app/trading/(components)/GridItem';

const Positions = () => {
  return <GridItem title="Positions" content={<div>Positions Table</div>} />;
};

export default Positions;
